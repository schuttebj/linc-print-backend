# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database dependencies
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication & Security (simplified for deployment)
python-jose[cryptography]==3.3.0
passlib==1.7.4
python-multipart==0.0.6

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# CORS and additional FastAPI features
python-dotenv==1.0.0

# File handling (removed Pillow and python-magic for now - can be added later if needed)
# These can be added when implementing document/image features:
# Pillow==10.1.0
# python-magic==0.4.27

# Additional utilities
email-validator==2.1.0

# Production server
gunicorn==21.2.0 